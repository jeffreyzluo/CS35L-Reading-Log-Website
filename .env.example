# Example .env file for local development
# Copy this to `.env` and fill in the real values before running the app.

# PostgreSQL connection
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=readinglog
PGPORT=5432

# App settings
PORT=3001
FRONTEND_ORIGIN=http://localhost:3000

# JWT secret used to sign authentication tokens
JWT_SECRET=replace_with_a_secure_random_value

# Third-party API keys
GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_BOOKS_API_KEY=your_google_books_api_key_here
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
# React needs the client id prefixed for the frontend env
REACT_APP_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# Notes:
# - Do NOT commit your real .env to source control. Keep secrets out of the repo.
# - After creating .env, ensure the DB user exists and PostgreSQL is running.
# - For Playwright: run `npx playwright install` once before running E2E tests.
